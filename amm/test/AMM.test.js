const AMM = artifacts.require("AMM");

contract("AMM", (accounts) => {
    it("Should put 1000 token1, 1000 token2 in a new AMM and receive shares", async () => {
        const ammInstance = await AMM.deployed();

        const 
    })
});